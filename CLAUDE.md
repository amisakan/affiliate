# CLAUDE.md

Claude Code は、affiliate-os のローカル修正、リファクタ、テスト、小規模実装を担当するAI開発エージェントです。

## プロジェクト哲学

affiliate-os は、信頼型アフィリエイト運用OSです。短期的な成約を煽るためではなく、読者が自分に合う選択をするための情報整理、比較、注意点の明示を支援します。

以下の方向に寄せないでください。

- 煽り系コピー
- 虚偽実績
- 誇大表現
- 不安商法
- 「誰でも簡単」「必ず稼げる」「月100万円確定」のような断定的な収益訴求

医療、転職、金融領域では、効果、成功、収益、安全性を断定しないでください。判断材料、確認観点、リスク、例外条件を丁寧に扱います。

## コードスタイル

- Python 3.11+ を前提にする
- 型ヒントを付ける
- データ構造は Pydantic model を優先する
- CLI は Typer、表示は Rich の既存方針に合わせる
- CSVや表形式データの処理は、必要に応じて pandas を使う
- 早すぎる抽象化を避け、MVPとして読みやすい実装を優先する

## 修正時のルール

- 変更範囲を小さく保つ
- 既存テストを読んでから実装する
- ユーザーや他エージェントの変更を勝手に戻さない
- 仕様が曖昧な場合は、読者保護とコンプライアンスを優先する
- 生成テキストやプロンプトには禁止表現を混ぜない

## テスト実行ルール

変更後は最低限、以下を実行してください。

```bash
make test
```

コード整形やLintに関わる変更をした場合は、以下も実行します。

```bash
make lint
```

## ファイル編集方針

- `affiliate_os/`: アプリケーションコード
- `tests/`: pytest テスト
- `prompts/`: AIエージェント用プロンプト
- `docs/`: 設計メモ、運用方針、仕様
- `data/`: 手元の入力データ
- `outputs/`: 生成物や分析結果

大きな機能は、まず `docs/` に短い仕様を書いてから実装してください。
